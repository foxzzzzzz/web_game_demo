# 项目协作规范

## Git 提交规范

- **每次 ****`git commit`**** 前必须先与 @我 确认**，不得自行提交。

    - 提交前增加一次 diff 级审核

- 确认内容包括：提交范围、commit message、是否创建 tag。

- 工作区代码可以随时修改和保存，仅 commit/push 动作需要审批。

- 不能使用git add \-A的方式添加代码， 一定要区分每次修改对应的文件，按相关文件添加的方式来进git add操作

- 根目录设立/tmp文件夹，用于保存各临时文件：临时日志，临时脚本，临时模拟数据等。此文件夹需要被加入到\.gitignore

## 代码规范

- 执行代码实施前，必须先输出对应的测试用例；在完成代码实现后，使用相关用例做功能验收和bug检查。

- 代码中涉及到常量配置参数的，不要写死在代码里面，而是通过外部config文件来动态配置，同时对于每个参数在config文件中做好含义说明。

## 文件规范

### docs/PRD\.md — 产品规格描述

- **独立新项目，没有完整的PRD\.md文件不能启动后续计划**

- 记录系统的产品规格、功能定义、设计决策。

- **每次规格变更时**，在文件顶部以表格形式记录版本变更：

```Markdown
| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v0.1 | 2026-07-09 | 初始版本 | — |
```

- 规格变更包括：新增功能模块、数据模型变更、API 接口变更、架构调整。

### docs/PLAN\.md— 产品实施计划描述

- 基于PRD做技术方案选型：实现框架/语言，重要接口定义，整体和各阶段验收标准；可优先完成MVP

    - 尝试去 GitHub 查找同类项目，基于其项目文档/代码，获取实现思路和方案；可以多套开源方案交叉对比，取长补短；合适的代码方案直接复用\(庞大的功能模块实现亦参考此原则\)

- 按 Phase1/Phase2\.\.\.的方式，分阶段设计完成计划和对应验收标准；涉及到前后关联的验收，也需要在实施合并后做验收；没有通过验收不能进行到下一阶段

### docs/CHANGELOG\.md — 问题修改记录 \+ 里程碑管理

- 按时间倒序记录重要修改。

- **里程碑节点使用 Git Tag 标记**，格式 `v0.1.0`、`v0.2.0`。

- 每次需要创建里程碑时，**提前与 @我 确认**后再打 tag。

- 里程碑划分建议，同步git tag的提交记录：

    - `v0.1.0` — Phase 1 \.\.\.

    - `v0.2.0` — Phase 2 \.\.\.

    - `v0.3.0` — Phase 3 \.\.\.

    - `v0.4.0` — Phase 4 \.\.\.

    - `v0.5.0` — Phase 5 \.\.\.

    - `v1.0.0` — 首次生产可用

### docs/Xx\.md — 专项问题分析记录

- 针对\*\*单一重要问题（预计处理跨度很大，代码量很多，影响范围很广的问题）\*\*的深度分析和处理过程记录。

- 命名示例：`docs/`[`anti-bot-analysis.md`](http://anti-bot-analysis.md)、`docs/`[`cost-formula-verification.md`](http://cost-formula-verification.md)。

- 内容包含：问题描述、分析过程、解决方案、验证结果。

- 文件名使用英文 kebab\-case，标题使用中文。

### docs/TodoList\.md — 待办任务清单

- 记录接下来需要完成的任务和优化项。

- 每条任务包含：**日期、任务描述、状态（待开始/进行中/已完成）、优先级**。

- 格式：

```Markdown
## 2026-07-09

| 状态 | 优先级 | 任务 |
|------|--------|------|
| ✅ 已完成 | P0 | 初始化项目 |
| 🔄 进行中 | P1 | ...... |
| ⏳ 待开始 | P2 | ...... |
```

- 完成任务后更新状态，**不要删除已完成条目**。

- 新增任务时追加在对应日期下。
